# Specification Pointer

The authoritative specification is [`grab-finance-copilot-pkg/SPEC.md`](grab-finance-copilot-pkg/SPEC.md).

The runnable application, datasets, tests, and evaluation harness all live inside `grab-finance-copilot-pkg/`. This root file exists only to prevent duplicate specifications from drifting.
